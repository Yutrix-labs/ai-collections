.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# Generated artifacts (reproducible via ptp-generate / ptp-train)
data/*.csv
models/*.joblib
models/*.json

# ...but DO commit the trained PTP model + metadata: they are baked into the
# Docker image (the source xlsx isn't in the repo, so we can't retrain at build).
!models/ptp_model.joblib
!models/model_metadata.json
