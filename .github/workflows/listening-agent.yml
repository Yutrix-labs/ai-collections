name: Listening Agent CI/CD
#updated file
# ci: manual redeploy trigger (ebix-demo)
on:
  push:
    branches: [ws-telephony]
    paths:
      - "backend/listening-agent/**"
      - "k8s/listening-agent/**"
      - "k8s/ws-bridge/**"
      - ".github/workflows/listening-agent.yml"
  pull_request:
    branches: [master]
    paths:
      - "backend/listening-agent/**"
  workflow_dispatch:

jobs:
  build-push:
    name: Build & Push Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image: ${{ steps.image.outputs.ref }}

    steps:
      - uses: actions/checkout@v4

      - name: Set image reference
        id: image
        run: |
          REPO=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          echo "ref=ghcr.io/${REPO}/listening-agent:${{ github.sha }}" >> "$GITHUB_OUTPUT"
          echo "latest=ghcr.io/${REPO}/listening-agent:latest"         >> "$GITHUB_OUTPUT"

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/listening-agent/Dockerfile
          push: ${{ github.ref == 'refs/heads/ws-telephony' && github.event_name == 'push' }}
          tags: |
            ${{ steps.image.outputs.ref }}
            ${{ steps.image.outputs.latest }}
          cache-from: type=registry,ref=${{ steps.image.outputs.latest }}
          cache-to: type=inline

  deploy:
    name: Deploy to K8s
    needs: build-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/ws-telephony' && github.event_name == 'push'
    # GITHUB_TOKEN needs packages:read so the cluster pull secret can be refreshed from it.
    permissions:
      contents: read
      packages: read

    steps:
      - uses: actions/checkout@v4

      - name: Pin image to SHA in manifest
        run: |
          sed -i "s|image: ghcr.io/yutrix-labs/ai-collections/listening-agent:.*|image: ${{ needs.build-push.outputs.image }}|" \
            k8s/listening-agent/deployment.yaml
          # ws-bridge runs the same image (ws_telephony_bridge.py ships in it) — pin it too.
          sed -i "s|image: ghcr.io/yutrix-labs/ai-collections/listening-agent:.*|image: ${{ needs.build-push.outputs.image }}|" \
            k8s/ws-bridge/deployment.yaml

      # Copy the (SHA-pinned) manifests to the VPS. We run kubectl ON the VPS over SSH because
      # GitHub-hosted runners can't reliably reach the k3s API on :6443 (i/o timeouts); locally
      # on the box the API is always reachable.
      - name: Copy manifests to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          source: "k8s/listening-agent/authz-policy.yaml,k8s/listening-agent/deployment.yaml,k8s/ws-bridge/peer-auth.yaml,k8s/ws-bridge/authz-policy.yaml,k8s/ws-bridge/deployment.yaml"
          target: "/tmp/ai-collections-listening-agent"
          overwrite: true

      - name: Apply on VPS over SSH
        uses: appleboy/ssh-action@v1.2.0
        env:
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          envs: GHCR_TOKEN
          script: |
            set -e
            cd /tmp/ai-collections-listening-agent
            # Refresh the GHCR pull secret so private images can be pulled (the previous token expired).
            kubectl -n app-services create secret docker-registry ghcr-pull-secret \
              --docker-server=ghcr.io \
              --docker-username=${{ github.actor }} \
              --docker-password="$GHCR_TOKEN" \
              --dry-run=client -o yaml | kubectl apply -f -
            # Istio resources are best-effort: istiod is scaled to 0 on this cluster (no sidecar
            # injection), so the validation webhook is down and CREATEs of mesh resources fail.
            kubectl apply -f k8s/listening-agent/authz-policy.yaml || echo "WARN: istio webhook down — authz-policy skipped (mesh disabled)"
            kubectl apply -f k8s/listening-agent/deployment.yaml
            # Tata telephony WebSocket bridge (same image, different entrypoint)
            kubectl apply -f k8s/ws-bridge/peer-auth.yaml || echo "WARN: istio webhook down — peer-auth skipped (mesh disabled)"
            kubectl apply -f k8s/ws-bridge/authz-policy.yaml || echo "WARN: istio webhook down — authz-policy skipped (mesh disabled)"
            kubectl apply -f k8s/ws-bridge/deployment.yaml
            kubectl -n app-services rollout status deployment/listening-agent --timeout=600s
            kubectl -n app-services rollout status deployment/ws-bridge --timeout=300s
