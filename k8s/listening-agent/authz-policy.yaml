apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: listening-agent-allow
  namespace: app-services
spec:
  selector:
    matchLabels:
      app: listening-agent
  rules:
  - {}
